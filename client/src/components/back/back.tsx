import React, { useState } from 'react';
import { Link } from 'react-router-dom';

function Back() {
    <Link className='befake' to="/" onClick={() => {setBack(false)}}>back</Link>
}

export default Back;

function setBack(arg0: boolean) {
    throw new Error('Function not implemented.');
}
